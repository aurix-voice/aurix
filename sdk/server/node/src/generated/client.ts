// Generated by tools/openapi-sdk/generate.py from api/openapi.json (Aurix 1.2.0). Do not edit.

/* eslint-disable */
import { AurixHttp, type RawResponse, type RequestOptions } from "../http.js";
import type * as T from "./types.js";

/**
 * Typed REST client for the Aurix control plane.
 *
 * Every method maps to one OpenAPI operation; see `T` for payload types.
 */
export class AurixClient extends AurixHttp {
  /**
   * Liveness and basic counters
   *
   * `GET /health`
   */
  health(options?: RequestOptions): Promise<T.Health> {
    return this.json<T.Health>("GET", "/health", { ...options });
  }

  /**
   * Readiness (database and Redis reachable)
   *
   * `GET /ready`
   */
  ready(options?: RequestOptions): Promise<T.Ready> {
    return this.json<T.Ready>("GET", "/ready", { ...options });
  }

  /**
   * This document
   *
   * `GET /openapi.json`
   */
  openapi(options?: RequestOptions): Promise<Record<string, unknown>> {
    return this.json<Record<string, unknown>>("GET", "/openapi.json", { ...options });
  }

  /**
   * Bootstrap the first superadmin
   *
   * Allowed only while no administrator exists, or when `X-Bootstrap-Token` matches
   * `auth.bootstrap_token`. Rate-limited by client IP.
   *
   * `POST /admin/setup`
   * Auth: BootstrapToken.
   */
  adminSetup(body: T.BootstrapAdminRequest, options?: RequestOptions): Promise<T.Admin> {
    return this.json<T.Admin>("POST", "/admin/setup", { body, ...options });
  }

  /**
   * Exchange email + password for an admin JWT
   *
   * `POST /admin/login`
   */
  adminLogin(body: T.AdminLoginRequest, options?: RequestOptions): Promise<T.AdminLoginResponse> {
    return this.json<T.AdminLoginResponse>("POST", "/admin/login", { body, ...options });
  }

  /**
   * Sign-in methods offered by this deployment
   *
   * For login pages: whether password login is on and whether (and where) SSO starts.
   *
   * `GET /admin/auth/methods`
   */
  adminAuthMethods(options?: RequestOptions): Promise<T.AdminAuthMethods> {
    return this.json<T.AdminAuthMethods>("GET", "/admin/auth/methods", { ...options });
  }

  /**
   * SSO callback (identity provider redirect target)
   *
   * Verifies `state` (encrypted, single-use, bound to the login cookie, expires after
   * `auth.oidc.state_ttl_secs`), exchanges `code` (PKCE), validates the ID token (signature via the
   * provider's JWKS, `iss`, `aud`, `nonce`, `exp`, email verification, allowed domains) and maps the
   * provider's groups to a role. With `auth.oidc.frontend_redirect` set, the browser is sent there
   * with `#token=…&expires_in_secs=…[&return_to=…]` (or `#error=…&error_description=…`); otherwise
   * the token is returned as JSON. The login cookie is cleared either way. Roles come from
   * `auth.oidc.superadmin_emails`, then the highest role in `role_mapping` matched by the groups
   * claim, then `default_role`; identities that resolve to no role are refused (`403`).
   *
   * `GET /admin/oidc/callback`
   */
  adminOidcCallback(query?: T.AdminOidcCallbackQuery, options?: RequestOptions): Promise<T.AdminOidcCallbackResponse> {
    return this.json<T.AdminOidcCallbackResponse>("GET", "/admin/oidc/callback", { query, ...options });
  }

  /**
   * Current administrator
   *
   * The account as stored now (role, permissions, SSO binding). `auth_source` reflects how the
   * presented token was obtained.
   *
   * `GET /admin/me`
   * Auth: AdminToken.
   */
  adminMe(options?: RequestOptions): Promise<T.Admin> {
    return this.json<T.Admin>("GET", "/admin/me", { ...options });
  }

  /**
   * Change own password
   *
   * Revokes every token issued to the caller so far, including the one used for this call — sign in
   * again afterwards.
   *
   * `POST /admin/me/password`
   * Auth: AdminToken.
   */
  adminChangeOwnPassword(body: T.ChangeOwnPasswordRequest, options?: RequestOptions): Promise<T.TokensRevoked> {
    return this.json<T.TokensRevoked>("POST", "/admin/me/password", { body, ...options });
  }

  /**
   * Sign out everywhere
   *
   * Revokes every token issued to the caller so far.
   *
   * `POST /admin/logout-all`
   * Auth: AdminToken.
   */
  adminLogoutAll(options?: RequestOptions): Promise<T.TokensRevoked> {
    return this.json<T.TokensRevoked>("POST", "/admin/logout-all", { ...options });
  }

  /**
   * List administrators
   *
   * `GET /admin/admins`
   * Auth: AdminToken.
   */
  listAdmins(options?: RequestOptions): Promise<T.ListAdminsResponse> {
    return this.json<T.ListAdminsResponse>("GET", "/admin/admins", { ...options });
  }

  /**
   * Create an administrator
   *
   * Creates a password account. SSO accounts are provisioned on their first SSO login instead
   * (`auth.oidc.auto_provision`).
   *
   * `POST /admin/admins`
   * Auth: AdminToken.
   */
  createAdmin(body: T.CreateAdminRequest, options?: RequestOptions): Promise<T.Admin> {
    return this.json<T.Admin>("POST", "/admin/admins", { body, ...options });
  }

  /**
   * Get an administrator
   *
   * `GET /admin/admins/{admin_id}`
   * Auth: AdminToken.
   */
  getAdmin(adminId: string, options?: RequestOptions): Promise<T.Admin> {
    return this.json<T.Admin>("GET", `/admin/admins/${encodeURIComponent(adminId)}`, { ...options });
  }

  /**
   * Update role, display name or active state
   *
   * `PATCH /admin/admins/{admin_id}`
   * Auth: AdminToken.
   */
  updateAdmin(adminId: string, body: T.UpdateAdminRequest, options?: RequestOptions): Promise<T.Admin> {
    return this.json<T.Admin>("PATCH", `/admin/admins/${encodeURIComponent(adminId)}`, { body, ...options });
  }

  /**
   * Reset an administrator's password
   *
   * Sets a new password (also for SSO-provisioned accounts) and revokes the target's tokens.
   *
   * `POST /admin/admins/{admin_id}/password`
   * Auth: AdminToken.
   */
  resetAdminPassword(adminId: string, body: T.ResetAdminPasswordRequest, options?: RequestOptions): Promise<T.TokensRevoked> {
    return this.json<T.TokensRevoked>("POST", `/admin/admins/${encodeURIComponent(adminId)}/password`, { body, ...options });
  }

  /**
   * Sign an administrator out everywhere
   *
   * Revokes every token issued to the target so far; the account stays active.
   *
   * `POST /admin/admins/{admin_id}/logout-all`
   * Auth: AdminToken.
   */
  revokeAdminTokens(adminId: string, options?: RequestOptions): Promise<T.TokensRevoked> {
    return this.json<T.TokensRevoked>("POST", `/admin/admins/${encodeURIComponent(adminId)}/logout-all`, { ...options });
  }

  /**
   * Audit log across all applications
   *
   * `GET /admin/audit-log`
   * Auth: AdminToken.
   */
  adminAuditLog(query?: T.AdminAuditLogQuery, options?: RequestOptions): Promise<T.AuditLogEntry[]> {
    return this.json<T.AuditLogEntry[]>("GET", "/admin/audit-log", { query, ...options });
  }

  /**
   * Run a retention sweep now
   *
   * Applies the `[retention]` rules immediately. The sweep holds a PostgreSQL advisory lock so only
   * one node runs it at a time.
   *
   * `POST /admin/retention/sweep`
   * Auth: AdminToken.
   */
  retentionSweep(options?: RequestOptions): Promise<T.SweepReport> {
    return this.json<T.SweepReport>("POST", "/admin/retention/sweep", { ...options });
  }

  /**
   * Usage totals of every application
   *
   * Range totals per application across the fleet (applications without usage in the range are
   * omitted). Default range: the last 30 days.
   *
   * `GET /admin/analytics/usage`
   * Auth: AdminToken.
   */
  adminUsage(query?: T.AdminUsageQuery, options?: RequestOptions): Promise<T.FleetUsage> {
    return this.json<T.FleetUsage>("GET", "/admin/analytics/usage", { query, ...options });
  }

  /**
   * One application's series, totals and quota state
   *
   * The same body as `GET /v1/analytics` for the given application, plus `app_id`, `active` and its
   * `quota`. Deactivated (deleted) applications are still returned so their last invoice can be
   * produced; `404` only for an unknown id.
   *
   * `GET /admin/analytics/apps/{app_id}`
   * Auth: AdminToken.
   */
  adminAppUsage(appId: string, query?: T.AdminAppUsageQuery, options?: RequestOptions): Promise<T.AdminAppUsage> {
    return this.json<T.AdminAppUsage>("GET", `/admin/analytics/apps/${encodeURIComponent(appId)}`, { query, ...options });
  }

  /**
   * 5-minute buckets of every application (JSON or CSV)
   *
   * Fleet-wide export of application buckets (`scope=fleet`), same row shape and cap as `GET
   * /v1/analytics/export`. Default range: the last 30 days.
   *
   * `GET /admin/analytics/export`
   * Auth: AdminToken.
   *
   * When the server answers with text/csv (non-JSON `format`) this call fails; use the `Raw` variant instead.
   */
  adminExportUsage(query?: T.AdminExportUsageQuery, options?: RequestOptions): Promise<T.UsageExport> {
    return this.json<T.UsageExport>("GET", "/admin/analytics/export", { query, ...options });
  }

  /**
   * 5-minute buckets of every application (JSON or CSV)
   *
   * Fleet-wide export of application buckets (`scope=fleet`), same row shape and cap as `GET
   * /v1/analytics/export`. Default range: the last 30 days.
   *
   * `GET /admin/analytics/export`
   * Auth: AdminToken.
   *
   * Returns the raw body (application/json, text/csv).
   */
  adminExportUsageRaw(query?: T.AdminExportUsageQuery, options?: RequestOptions): Promise<RawResponse> {
    return this.raw("GET", "/admin/analytics/export", { query, ...options });
  }

  /**
   * List applications
   *
   * `GET /v1/apps`
   * Auth: AdminToken.
   */
  listApps(query?: T.ListAppsQuery, options?: RequestOptions): Promise<T.ListAppsResponse> {
    return this.json<T.ListAppsResponse>("GET", "/v1/apps", { query, ...options });
  }

  /**
   * Create an application (tenant)
   *
   * Returns the application's first API key with all permissions; it is shown only once.
   *
   * `POST /v1/apps`
   * Auth: AdminToken.
   */
  createApp(body: T.CreateAppRequest, options?: RequestOptions): Promise<T.CreatedApp> {
    return this.json<T.CreatedApp>("POST", "/v1/apps", { body, ...options });
  }

  /**
   * Application with channel and session counts
   *
   * `GET /v1/apps/{app_id}`
   * Auth: AdminToken.
   */
  getApp(appId: string, options?: RequestOptions): Promise<T.AppDetail> {
    return this.json<T.AppDetail>("GET", `/v1/apps/${encodeURIComponent(appId)}`, { ...options });
  }

  /**
   * Edit an application's name, description or quotas
   *
   * Fields left out are unchanged; `description: null` clears it. Raising
   * `max_participants_per_channel` is how an existing application gets large (audience) channels —
   * `POST /v1/channels` clamps `config.max_participants` to this quota. Lowering it affects channels
   * created or reconfigured afterwards. `max_concurrent_sessions` and `monthly_participant_minutes`
   * (0 = unlimited) take effect on new sessions/joins within `usage.quota_cache_secs`; existing
   * sessions are not cut.
   *
   * `PATCH /v1/apps/{app_id}`
   * Auth: AdminToken.
   */
  updateApp(appId: string, body: T.UpdateAppRequest, options?: RequestOptions): Promise<T.App> {
    return this.json<T.App>("PATCH", `/v1/apps/${encodeURIComponent(appId)}`, { body, ...options });
  }

  /**
   * Deactivate an application
   *
   * Soft-deletes the app: its API keys stop authenticating at once, every live session is closed on
   * every node, and its usage history stays readable through `/admin/analytics`. Deleting an already
   * deactivated or unknown application returns 404.
   *
   * `DELETE /v1/apps/{app_id}`
   * Auth: AdminToken.
   */
  deleteApp(appId: string, options?: RequestOptions): Promise<T.DeleteAppResponse> {
    return this.json<T.DeleteAppResponse>("DELETE", `/v1/apps/${encodeURIComponent(appId)}`, { ...options });
  }

  /**
   * Issue a new full-permission API key for an application
   *
   * Existing keys keep working until revoked through `DELETE /v1/api-keys/{key_id}`.
   *
   * `POST /v1/apps/{app_id}/rotate-key`
   * Auth: AdminToken.
   */
  rotateAppKey(appId: string, options?: RequestOptions): Promise<T.RotateAppKeyResponse> {
    return this.json<T.RotateAppKeyResponse>("POST", `/v1/apps/${encodeURIComponent(appId)}/rotate-key`, { ...options });
  }

  /**
   * Media node fleet
   *
   * Every node registers itself and heartbeats; nodes silent for 30 s are marked unhealthy.
   *
   * `GET /v1/nodes`
   * Auth: AdminToken.
   */
  listNodes(options?: RequestOptions): Promise<T.MediaNode[]> {
    return this.json<T.MediaNode[]>("GET", "/v1/nodes", { ...options });
  }

  /**
   * Issue a player session token
   *
   * Creates or looks up the user by `external_id` and returns a session JWT with the channel grants.
   * Call this from your game backend; never ship API keys to clients.
   *
   * `POST /v1/tokens`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: tokens:issue.
   */
  issueToken(body: T.GenerateTokenRequest, options?: RequestOptions): Promise<T.TokenResponse> {
    return this.json<T.TokenResponse>("POST", "/v1/tokens", { body, ...options });
  }

  /**
   * Issue a one-time action token
   *
   * `login` opens a session, `join` enters one channel, `kick`/`mute`/`unmute` authorise a single
   * moderation act (these three additionally need `moderation:write`). Each token is bound to a
   * `jti` that is consumed atomically on first use. With `auth.require_action_tokens = true` session
   * JWTs alone can no longer open sessions or join channels.
   *
   * `POST /v1/tokens/action`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: tokens:issue, moderation:write (kick/mute/unmute).
   */
  issueActionToken(body: T.ActionTokenRequest, options?: RequestOptions): Promise<T.ActionTokenResponse> {
    return this.json<T.ActionTokenResponse>("POST", "/v1/tokens/action", { body, ...options });
  }

  /**
   * Region discovery for the game backend
   *
   * One entry per region with at least one healthy, non-saturated node that advertises a public
   * WebSocket URL, carrying that region's least-loaded node. Use it to choose the region before
   * minting a token, or pass `region`/`location` straight to `POST /v1/tokens` and read `endpoint`.
   * Served from the node registry, which follows the fleet's heartbeats with at most ~15 s of lag.
   *
   * `GET /v1/regions`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: tokens:issue.
   */
  listRegions(query?: T.ListRegionsQuery, options?: RequestOptions): Promise<T.RegionsResponse> {
    return this.json<T.RegionsResponse>("GET", "/v1/regions", { query, ...options });
  }

  /**
   * TURN credentials for a user (server-to-server)
   *
   * `POST /v1/turn/credentials`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: turn:issue.
   */
  issueTurnCredentials(body: T.TurnCredentialsRequest, options?: RequestOptions): Promise<T.TurnCredentials> {
    return this.json<T.TurnCredentials>("POST", "/v1/turn/credentials", { body, ...options });
  }

  /**
   * List channels
   *
   * `GET /v1/channels`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: channels:read.
   */
  listChannels(query?: T.ListChannelsQuery, options?: RequestOptions): Promise<T.ChannelList> {
    return this.json<T.ChannelList>("GET", "/v1/channels", { query, ...options });
  }

  /**
   * Create a channel
   *
   * Fails with `409` when the application's `max_channels` quota is reached.
   *
   * `POST /v1/channels`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: channels:write.
   */
  createChannel(body: T.CreateChannelRequest, options?: RequestOptions): Promise<T.Channel> {
    return this.json<T.Channel>("POST", "/v1/channels", { body, ...options });
  }

  /**
   * Get a channel
   *
   * `GET /v1/channels/{channel_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: channels:read.
   */
  getChannel(channelId: string, options?: RequestOptions): Promise<T.Channel> {
    return this.json<T.Channel>("GET", `/v1/channels/${encodeURIComponent(channelId)}`, { ...options });
  }

  /**
   * Delete a channel
   *
   * Soft-deletes the channel and disconnects its participants.
   *
   * `DELETE /v1/channels/{channel_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: channels:write.
   */
  deleteChannel(channelId: string, options?: RequestOptions): Promise<T.DeleteChannelResponse> {
    return this.json<T.DeleteChannelResponse>("DELETE", `/v1/channels/${encodeURIComponent(channelId)}`, { ...options });
  }

  /**
   * Replace the channel configuration
   *
   * The full `ChannelConfig` is replaced (omitted fields revert to defaults). Live participants keep
   * their session; new settings apply to subsequent joins.
   *
   * `PUT /v1/channels/{channel_id}/config`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: channels:write.
   */
  updateChannelConfig(channelId: string, body: T.ChannelConfig, options?: RequestOptions): Promise<T.Channel> {
    return this.json<T.Channel>("PUT", `/v1/channels/${encodeURIComponent(channelId)}/config`, { body, ...options });
  }

  /**
   * Participants of a channel
   *
   * `memberships` come from the database (all nodes); `live_on_this_node` adds live SFU state for
   * sessions hosted by the answering node.
   *
   * `GET /v1/channels/{channel_id}/participants`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: channels:read.
   */
  getChannelParticipants(channelId: string, options?: RequestOptions): Promise<T.ChannelParticipants> {
    return this.json<T.ChannelParticipants>("GET", `/v1/channels/${encodeURIComponent(channelId)}/participants`, { ...options });
  }

  /**
   * Live statistics of a session
   *
   * Node-local: the session must be hosted by the node that answers (its `media_node_id` is in `GET
   * /v1/users/{user_id}`); otherwise `404`.
   *
   * `GET /v1/sessions/{session_id}/stats`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: channels:read.
   */
  getSessionStats(sessionId: string, options?: RequestOptions): Promise<T.SessionStats> {
    return this.json<T.SessionStats>("GET", `/v1/sessions/${encodeURIComponent(sessionId)}/stats`, { ...options });
  }

  /**
   * Search users
   *
   * `GET /v1/users`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: users:read.
   */
  searchUsers(query?: T.SearchUsersQuery, options?: RequestOptions): Promise<T.User[]> {
    return this.json<T.User[]>("GET", "/v1/users", { query, ...options });
  }

  /**
   * User with active sessions
   *
   * `GET /v1/users/{user_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: users:read.
   */
  getUser(userId: string, options?: RequestOptions): Promise<T.UserDetail> {
    return this.json<T.UserDetail>("GET", `/v1/users/${encodeURIComponent(userId)}`, { ...options });
  }

  /**
   * Erase a user
   *
   * Removes recordings, sessions, memberships, chat, blocks and the user row in one transaction,
   * closes live sessions on every node and writes a tombstone so tokens issued before the deletion
   * are rejected. Moderation evidence is kept unless `purge_moderation=true`.
   *
   * `DELETE /v1/users/{user_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: users:erase.
   */
  deleteUser(userId: string, query?: T.DeleteUserQuery, options?: RequestOptions): Promise<T.UserDeletion> {
    return this.json<T.UserDeletion>("DELETE", `/v1/users/${encodeURIComponent(userId)}`, { query, ...options });
  }

  /**
   * Export everything stored about a user
   *
   * `GET /v1/users/{user_id}/export`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: users:export.
   */
  exportUser(userId: string, options?: RequestOptions): Promise<T.UserExport> {
    return this.json<T.UserExport>("GET", `/v1/users/${encodeURIComponent(userId)}/export`, { ...options });
  }

  /**
   * Revoke all active bans of a user
   *
   * `POST /v1/users/{user_id}/unban`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  unbanUser(userId: string, body?: T.UnbanRequest, options?: RequestOptions): Promise<T.UnbanUserResponse> {
    return this.json<T.UnbanUserResponse>("POST", `/v1/users/${encodeURIComponent(userId)}/unban`, { body, ...options });
  }

  /**
   * Users this user has blocked
   *
   * `GET /v1/users/{user_id}/blocks`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: users:read.
   */
  listUserBlocks(userId: string, options?: RequestOptions): Promise<T.UserBlocks> {
    return this.json<T.UserBlocks>("GET", `/v1/users/${encodeURIComponent(userId)}/blocks`, { ...options });
  }

  /**
   * Block another user
   *
   * Persistent, mutual: neither side hears or reads the other, on every node and after reconnects.
   *
   * `POST /v1/users/{user_id}/blocks`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: users:write.
   */
  addUserBlock(userId: string, body: T.BlockRequest, options?: RequestOptions): Promise<T.BlockResult> {
    return this.json<T.BlockResult>("POST", `/v1/users/${encodeURIComponent(userId)}/blocks`, { body, ...options });
  }

  /**
   * Remove a block
   *
   * `DELETE /v1/users/{user_id}/blocks/{blocked_user_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: users:write.
   */
  removeUserBlock(userId: string, blockedUserId: string, options?: RequestOptions): Promise<T.BlockResult> {
    return this.json<T.BlockResult>("DELETE", `/v1/users/${encodeURIComponent(userId)}/blocks/${encodeURIComponent(blockedUserId)}`, { ...options });
  }

  /**
   * Channel message history
   *
   * History endpoints require `chat.persist = true` on the deployment (otherwise `404`). Pages are
   * newest first; follow `next_before` to older messages and `next_after` to newer ones (absent when
   * there is nothing more in that direction).
   *
   * `GET /v1/channels/{channel_id}/messages`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: chat:read.
   */
  listChannelMessages(channelId: string, query?: T.ListChannelMessagesQuery, options?: RequestOptions): Promise<T.ChatHistoryPage> {
    return this.json<T.ChatHistoryPage>("GET", `/v1/channels/${encodeURIComponent(channelId)}/messages`, { query, ...options });
  }

  /**
   * Send a system message to a channel
   *
   * Delivered live to every participant as `ChatMessageReceived` from the system user.
   *
   * `POST /v1/channels/{channel_id}/messages`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: chat:write.
   */
  sendChannelMessage(channelId: string, body: T.SystemMessageRequest, options?: RequestOptions): Promise<T.ChatMessage> {
    return this.json<T.ChatMessage>("POST", `/v1/channels/${encodeURIComponent(channelId)}/messages`, { body, ...options });
  }

  /**
   * Directed message history of a user
   *
   * History endpoints require `chat.persist = true` on the deployment (otherwise `404`). Pages are
   * newest first; follow `next_before` to older messages and `next_after` to newer ones (absent when
   * there is nothing more in that direction).
   *
   * `GET /v1/users/{user_id}/messages`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: chat:read.
   */
  listUserMessages(userId: string, query?: T.ListUserMessagesQuery, options?: RequestOptions): Promise<T.ChatHistoryPage> {
    return this.json<T.ChatHistoryPage>("GET", `/v1/users/${encodeURIComponent(userId)}/messages`, { query, ...options });
  }

  /**
   * Send a system message to one online user
   *
   * The user must currently have a live session (no offline delivery).
   *
   * `POST /v1/users/{user_id}/messages`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: chat:write.
   */
  sendUserMessage(userId: string, body: T.SystemMessageRequest, options?: RequestOptions): Promise<T.ChatMessage> {
    return this.json<T.ChatMessage>("POST", `/v1/users/${encodeURIComponent(userId)}/messages`, { body, ...options });
  }

  /**
   * Speak an announcement into a channel
   *
   * Synthesises `text` with the configured `[tts]` provider and injects it as a system voice
   * participant; returns as soon as the request is queued (`501` if no provider is configured).
   *
   * `POST /v1/channels/{channel_id}/tts`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: tts:write.
   */
  announceInChannel(channelId: string, body: T.AnnounceRequest, options?: RequestOptions): Promise<T.AnnounceInChannelResponse> {
    return this.json<T.AnnounceInChannelResponse>("POST", `/v1/channels/${encodeURIComponent(channelId)}/tts`, { body, ...options });
  }

  /**
   * Available TTS voices and limits
   *
   * `GET /v1/tts/voices`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: channels:read.
   */
  ttsVoices(options?: RequestOptions): Promise<T.TtsVoicesResponse> {
    return this.json<T.TtsVoicesResponse>("GET", "/v1/tts/voices", { ...options });
  }

  /**
   * Ban a user, device or IP
   *
   * Live sessions of the user are closed on every node.
   *
   * `POST /v1/moderation/ban`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  banUser(body: T.BanRequest, options?: RequestOptions): Promise<T.Ban> {
    return this.json<T.Ban>("POST", "/v1/moderation/ban", { body, ...options });
  }

  /**
   * List bans
   *
   * `GET /v1/moderation/bans`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:read.
   */
  listBans(query?: T.ListBansQuery, options?: RequestOptions): Promise<T.Ban[]> {
    return this.json<T.Ban[]>("GET", "/v1/moderation/bans", { query, ...options });
  }

  /**
   * Revoke one ban
   *
   * `POST /v1/moderation/bans/{ban_id}/revoke`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  revokeBan(banId: string, body?: T.UnbanRequest, options?: RequestOptions): Promise<T.RevokeBanResponse> {
    return this.json<T.RevokeBanResponse>("POST", `/v1/moderation/bans/${encodeURIComponent(banId)}/revoke`, { body, ...options });
  }

  /**
   * Server-mute or unmute a participant
   *
   * `POST /v1/moderation/mute`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  serverMute(body: T.ServerMuteRequest, options?: RequestOptions): Promise<T.ServerMuteResponse> {
    return this.json<T.ServerMuteResponse>("POST", "/v1/moderation/mute", { body, ...options });
  }

  /**
   * Server-mute or unmute everyone in a channel
   *
   * `POST /v1/moderation/mute-all`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  muteAll(body: T.MuteAllRequest, options?: RequestOptions): Promise<T.BulkModerationResult> {
    return this.json<T.BulkModerationResult>("POST", "/v1/moderation/mute-all", { body, ...options });
  }

  /**
   * Promote or demote a priority speaker
   *
   * Sets the priority-speaker flag on the user's open membership in the channel and notifies every
   * member (`PriorityChanged`). While a priority speaker talks, non-priority streams are attenuated
   * per `ChannelConfig.ducking`. `404` when the user is not currently a member.
   *
   * `POST /v1/moderation/priority`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  setPriority(body: T.SetPriorityRequest, options?: RequestOptions): Promise<T.SetPriorityResponse> {
    return this.json<T.SetPriorityResponse>("POST", "/v1/moderation/priority", { body, ...options });
  }

  /**
   * Kick a participant from a channel
   *
   * `POST /v1/moderation/kick`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  kickUser(body: T.KickRequest, options?: RequestOptions): Promise<T.KickUserResponse> {
    return this.json<T.KickUserResponse>("POST", "/v1/moderation/kick", { body, ...options });
  }

  /**
   * Kick everyone from a channel
   *
   * `POST /v1/moderation/kick-all`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  kickAll(body: T.KickAllRequest, options?: RequestOptions): Promise<T.BulkModerationResult> {
    return this.json<T.BulkModerationResult>("POST", "/v1/moderation/kick-all", { body, ...options });
  }

  /**
   * File a report on behalf of a user
   *
   * `POST /v1/moderation/report`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  reportUser(body: T.ReportRequest, options?: RequestOptions): Promise<T.ModerationEvent> {
    return this.json<T.ModerationEvent>("POST", "/v1/moderation/report", { body, ...options });
  }

  /**
   * List moderation events (reports, bans, kicks…)
   *
   * `GET /v1/moderation/events`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:read.
   */
  listModerationEvents(query?: T.ListModerationEventsQuery, options?: RequestOptions): Promise<T.ModerationEvent[]> {
    return this.json<T.ModerationEvent[]>("GET", "/v1/moderation/events", { query, ...options });
  }

  /**
   * Get a moderation event
   *
   * `GET /v1/moderation/events/{event_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:read.
   */
  getModerationEvent(eventId: string, options?: RequestOptions): Promise<T.ModerationEvent> {
    return this.json<T.ModerationEvent>("GET", `/v1/moderation/events/${encodeURIComponent(eventId)}`, { ...options });
  }

  /**
   * Resolve a moderation event
   *
   * `POST /v1/moderation/events/{event_id}/resolve`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:write.
   */
  resolveModerationEvent(eventId: string, body: T.ResolveModerationEventRequest, options?: RequestOptions): Promise<T.ResolveModerationEventResponse> {
    return this.json<T.ResolveModerationEventResponse>("POST", `/v1/moderation/events/${encodeURIComponent(eventId)}/resolve`, { body, ...options });
  }

  /**
   * List content-safety incidents
   *
   * Incidents raised by the `[safety]` pipeline. They are moderation events with `event_type`
   * `safety.voice` or `safety.text` (so `POST /v1/moderation/events/{event_id}/resolve` closes
   * them); `evidence` holds the score, categories, flagged text, classifier and lexicon verdicts,
   * context messages and the evidence-clip metadata. Newest first.
   *
   * `GET /v1/safety/incidents`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:read.
   */
  listSafetyIncidents(query?: T.ListSafetyIncidentsQuery, options?: RequestOptions): Promise<T.ModerationEvent[]> {
    return this.json<T.ModerationEvent[]>("GET", "/v1/safety/incidents", { query, ...options });
  }

  /**
   * Get a content-safety incident
   *
   * The incident with its context messages and, for voice incidents with a stored clip, the
   * recording metadata and the path that streams it (`recordings:read`).
   *
   * `GET /v1/safety/incidents/{incident_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:read.
   */
  getSafetyIncident(incidentId: string, options?: RequestOptions): Promise<T.SafetyIncident> {
    return this.json<T.SafetyIncident>("GET", `/v1/safety/incidents/${encodeURIComponent(incidentId)}`, { ...options });
  }

  /**
   * Export an incident with its evidence
   *
   * Self-contained JSON bundle for hand-off to a trust & safety team: the incident, the surrounding
   * chat messages and — when a clip exists **and the key also holds `recordings:read`** — the
   * decrypted Ogg/Opus audio inline as `audio.content_base64`. Reading the clip is written to the
   * audit log as `recording.accessed`. Without `recordings:read` the bundle is returned without the
   * audio bytes.
   *
   * `GET /v1/safety/incidents/{incident_id}/export`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:read.
   */
  exportSafetyIncident(incidentId: string, options?: RequestOptions): Promise<T.SafetyIncidentExport> {
    return this.json<T.SafetyIncidentExport>("GET", `/v1/safety/incidents/${encodeURIComponent(incidentId)}/export`, { ...options });
  }

  /**
   * Current risk of a user
   *
   * Decayed sum of the user's recent incident scores (`[safety].risk_half_life_secs`), the same
   * value that drives automatic mute/kick triggers and `safety.risk_changed` events. Computed from
   * stored incidents, identical on every node.
   *
   * `GET /v1/safety/users/{user_id}/risk`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: moderation:read.
   */
  getSafetyUserRisk(userId: string, options?: RequestOptions): Promise<T.SafetyRisk> {
    return this.json<T.SafetyRisk>("GET", `/v1/safety/users/${encodeURIComponent(userId)}/risk`, { ...options });
  }

  /**
   * Current counters, range totals and the application time series
   *
   * CCU (`peak_sessions`), session and participant minutes, unique users, active channels and
   * metered counters per bucket. Lifecycle metrics (CCU, minutes, unique users) are derived from
   * session and membership intervals by a fleet-wide aggregator that runs every
   * `usage.aggregate_interval_secs`; metered counters (media bytes, chat, TTS, STT) are flushed by
   * every node every `usage.flush_interval_secs`. Buckets before `range.finalized_through` are
   * final; the current one is still accruing. Clock-time is UTC; buckets are aligned to epoch
   * multiples of the step.
   *
   * `GET /v1/analytics`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: analytics:read.
   */
  getAnalytics(query?: T.GetAnalyticsQuery, options?: RequestOptions): Promise<T.AppUsage> {
    return this.json<T.AppUsage>("GET", "/v1/analytics", { query, ...options });
  }

  /**
   * Limits of the application and how much of them is used
   *
   * `max_concurrent_sessions` and `monthly_participant_minutes` are set by the operator (`PATCH
   * /v1/apps/{app_id}`); `0` means unlimited. Consumption is fleet-wide.
   *
   * `GET /v1/analytics/quota`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: analytics:read.
   */
  getQuota(options?: RequestOptions): Promise<T.QuotaState> {
    return this.json<T.QuotaState>("GET", "/v1/analytics/quota", { ...options });
  }

  /**
   * Sessions with the worst MOS over a range
   *
   * Sessions (live and closed) that connected in the range, ordered by mean MOS ascending, each with
   * its persisted `QualitySummary`. Live sessions show the summary as of their last checkpoint
   * (`quality.persist_interval_secs`); `GET /v1/sessions/{session_id}/stats` has the live value. Use
   * this to find who had a bad call instead of high-cardinality metrics — Prometheus only carries
   * fleet-wide distributions.
   *
   * `GET /v1/analytics/sessions`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: analytics:read.
   */
  listSessionQuality(query?: T.ListSessionQualityQuery, options?: RequestOptions): Promise<T.SessionQualityList> {
    return this.json<T.SessionQualityList>("GET", "/v1/analytics/sessions", { query, ...options });
  }

  /**
   * Per-channel totals over a range, busiest first
   *
   * Channels that had any usage in the range (including deleted ones — usage outlives the channel),
   * ordered by participant minutes.
   *
   * `GET /v1/analytics/channels`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: analytics:read.
   */
  listChannelUsage(query?: T.ListChannelUsageQuery, options?: RequestOptions): Promise<T.ChannelUsageList> {
    return this.json<T.ChannelUsageList>("GET", "/v1/analytics/channels", { query, ...options });
  }

  /**
   * Hourly series of one channel
   *
   * Peak participants, participant minutes, joins, unique users and metered counters per hour. A
   * channel that neither exists in the application nor has usage there is `404`.
   *
   * `GET /v1/analytics/channels/{channel_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: analytics:read.
   */
  getChannelUsage(channelId: string, query?: T.GetChannelUsageQuery, options?: RequestOptions): Promise<T.ChannelUsage> {
    return this.json<T.ChannelUsage>("GET", `/v1/analytics/channels/${encodeURIComponent(channelId)}`, { query, ...options });
  }

  /**
   * Raw usage buckets for billing or BI (JSON or CSV)
   *
   * Every bucket of the application in the range: 5-minute application rows (`scope=app`, default)
   * or hourly per-channel rows (`scope=channels`). At most 200 000 rows per call — `truncated`
   * (JSON) / `X-Aurix-Truncated` (CSV) signals a cut; page by narrowing `from`/`to`. Default range:
   * the last 30 days.
   *
   * `GET /v1/analytics/export`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: analytics:read.
   *
   * When the server answers with text/csv (non-JSON `format`) this call fails; use the `Raw` variant instead.
   */
  exportUsage(query?: T.ExportUsageQuery, options?: RequestOptions): Promise<T.UsageExport> {
    return this.json<T.UsageExport>("GET", "/v1/analytics/export", { query, ...options });
  }

  /**
   * Raw usage buckets for billing or BI (JSON or CSV)
   *
   * Every bucket of the application in the range: 5-minute application rows (`scope=app`, default)
   * or hourly per-channel rows (`scope=channels`). At most 200 000 rows per call — `truncated`
   * (JSON) / `X-Aurix-Truncated` (CSV) signals a cut; page by narrowing `from`/`to`. Default range:
   * the last 30 days.
   *
   * `GET /v1/analytics/export`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: analytics:read.
   *
   * Returns the raw body (application/json, text/csv).
   */
  exportUsageRaw(query?: T.ExportUsageQuery, options?: RequestOptions): Promise<RawResponse> {
    return this.raw("GET", "/v1/analytics/export", { query, ...options });
  }

  /**
   * List API keys (secrets redacted)
   *
   * `GET /v1/api-keys`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: keys:manage.
   */
  listApiKeys(options?: RequestOptions): Promise<T.ApiKey[]> {
    return this.json<T.ApiKey[]>("GET", "/v1/api-keys", { ...options });
  }

  /**
   * Create an API key
   *
   * A key can only grant permissions it holds itself. The raw key is returned once.
   *
   * `POST /v1/api-keys`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: keys:manage.
   */
  createApiKey(body: T.CreateApiKeyRequest, options?: RequestOptions): Promise<T.CreatedApiKey> {
    return this.json<T.CreatedApiKey>("POST", "/v1/api-keys", { body, ...options });
  }

  /**
   * Change an API key's request budget
   *
   * Takes effect on the next request on every node (buckets are shared through Redis).
   *
   * `PATCH /v1/api-keys/{key_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: keys:manage.
   */
  updateApiKey(keyId: string, body: T.UpdateApiKeyRequest, options?: RequestOptions): Promise<T.ApiKey> {
    return this.json<T.ApiKey>("PATCH", `/v1/api-keys/${encodeURIComponent(keyId)}`, { body, ...options });
  }

  /**
   * Revoke an API key
   *
   * `DELETE /v1/api-keys/{key_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: keys:manage.
   */
  revokeApiKey(keyId: string, options?: RequestOptions): Promise<T.RevokeApiKeyResponse> {
    return this.json<T.RevokeApiKeyResponse>("DELETE", `/v1/api-keys/${encodeURIComponent(keyId)}`, { ...options });
  }

  /**
   * Audit log of this application
   *
   * Hash-chained; every privileged action (keys, bans, kicks, recording access, …) is recorded.
   *
   * `GET /v1/audit-log`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: audit:read.
   */
  listAuditLog(query?: T.ListAuditLogQuery, options?: RequestOptions): Promise<T.AuditLogEntry[]> {
    return this.json<T.AuditLogEntry[]>("GET", "/v1/audit-log", { query, ...options });
  }

  /**
   * List recordings
   *
   * `GET /v1/recordings`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:read.
   */
  listRecordings(query?: T.ListRecordingsQuery, options?: RequestOptions): Promise<T.Recording[]> {
    return this.json<T.Recording[]>("GET", "/v1/recordings", { query, ...options });
  }

  /**
   * Start recording a participant
   *
   * Requires `recording.enabled` on the node and the participant to be live on it. With
   * `recording.require_consent` audio is written only after the participant accepts the
   * `RecordingNotification`.
   *
   * `POST /v1/recordings/start`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:write.
   */
  startRecording(body: T.StartRecordingRequest, options?: RequestOptions): Promise<T.Recording> {
    return this.json<T.Recording>("POST", "/v1/recordings/start", { body, ...options });
  }

  /**
   * Recording metadata, download URL and consent state
   *
   * `GET /v1/recordings/{recording_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:read.
   */
  getRecording(recordingId: string, options?: RequestOptions): Promise<T.GetRecordingResponse> {
    return this.json<T.GetRecordingResponse>("GET", `/v1/recordings/${encodeURIComponent(recordingId)}`, { ...options });
  }

  /**
   * Delete a recording and its file
   *
   * `DELETE /v1/recordings/{recording_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:write.
   */
  deleteRecording(recordingId: string, options?: RequestOptions): Promise<T.DeleteRecordingResponse> {
    return this.json<T.DeleteRecordingResponse>("DELETE", `/v1/recordings/${encodeURIComponent(recordingId)}`, { ...options });
  }

  /**
   * Download the recording file
   *
   * Decrypted on the fly when recording encryption is enabled. Access is written to the audit log.
   * Participant tracks and Opus mixdowns are Ogg/Opus, WAV mixdowns are 16-bit PCM. 409 while the
   * recording is still `recording`/`processing` or has `failed`.
   *
   * `GET /v1/recordings/{recording_id}/download`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:read.
   *
   * Returns the raw body (audio/ogg, audio/wav).
   */
  downloadRecordingRaw(recordingId: string, options?: RequestOptions): Promise<RawResponse> {
    return this.raw("GET", `/v1/recordings/${encodeURIComponent(recordingId)}/download`, { ...options });
  }

  /**
   * Stop a running recording
   *
   * `POST /v1/recordings/{recording_id}/stop`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:write.
   */
  stopRecording(recordingId: string, options?: RequestOptions): Promise<T.Recording> {
    return this.json<T.Recording>("POST", `/v1/recordings/${encodeURIComponent(recordingId)}/stop`, { ...options });
  }

  /**
   * Live streams of this application on this node
   *
   * `GET /v1/audio/streams`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: audio_streams:read.
   */
  listStreams(options?: RequestOptions): Promise<T.ListStreamsResponse> {
    return this.json<T.ListStreamsResponse>("GET", "/v1/audio/streams", { ...options });
  }

  /**
   * Live streams of a channel on this node
   *
   * `GET /v1/channels/{channel_id}/audio/streams`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: audio_streams:read.
   */
  listChannelStreams(channelId: string, options?: RequestOptions): Promise<T.ListChannelStreamsResponse> {
    return this.json<T.ListChannelStreamsResponse>("GET", `/v1/channels/${encodeURIComponent(channelId)}/audio/streams`, { ...options });
  }

  /**
   * Start a push stream
   *
   * Aurix connects to your WebSocket endpoint and pushes per-participant Opus or PCM frames (36-byte
   * binary header) plus JSON control frames. Streams are node-local: address the node that hosts
   * participants of the channel (`409` otherwise).
   *
   * `POST /v1/channels/{channel_id}/audio/streams`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: audio_streams:write.
   */
  createStream(channelId: string, body: T.CreateStreamRequest, options?: RequestOptions): Promise<T.LiveStream> {
    return this.json<T.LiveStream>("POST", `/v1/channels/${encodeURIComponent(channelId)}/audio/streams`, { body, ...options });
  }

  /**
   * Get a live stream
   *
   * `GET /v1/channels/{channel_id}/audio/streams/{stream_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: audio_streams:read.
   */
  getStream(channelId: string, streamId: string, options?: RequestOptions): Promise<T.LiveStream> {
    return this.json<T.LiveStream>("GET", `/v1/channels/${encodeURIComponent(channelId)}/audio/streams/${encodeURIComponent(streamId)}`, { ...options });
  }

  /**
   * Stop a live stream
   *
   * `DELETE /v1/channels/{channel_id}/audio/streams/{stream_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: audio_streams:write.
   */
  deleteStream(channelId: string, streamId: string, options?: RequestOptions): Promise<T.LiveStream> {
    return this.json<T.LiveStream>("DELETE", `/v1/channels/${encodeURIComponent(channelId)}/audio/streams/${encodeURIComponent(streamId)}`, { ...options });
  }

  /**
   * List webhook subscriptions
   *
   * `GET /v1/webhooks`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:read.
   */
  listWebhooks(options?: RequestOptions): Promise<T.ListWebhooksResponse> {
    return this.json<T.ListWebhooksResponse>("GET", "/v1/webhooks", { ...options });
  }

  /**
   * Create a webhook subscription
   *
   * The response includes `secret` (once). Deliveries are `POST`s with the `EventEnvelope` body and
   * `X-Aurix-Signature: t=<unix>,v1=hex(HMAC-SHA256(secret, "<t>.<body>"))`; failures retry from 5 s
   * up to 2 h.
   *
   * `POST /v1/webhooks`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:write.
   */
  createWebhook(body: T.CreateWebhookRequest, options?: RequestOptions): Promise<T.Webhook> {
    return this.json<T.Webhook>("POST", "/v1/webhooks", { body, ...options });
  }

  /**
   * Event types and the signature scheme
   *
   * `webhook` lists what a subscription may ask for, `stream` what the SSE stream can deliver (adds
   * the high-frequency typing/speaking/energy events).
   *
   * `GET /v1/webhooks/events`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:read, events:read (either).
   */
  listEventTypes(options?: RequestOptions): Promise<T.EventTypes> {
    return this.json<T.EventTypes>("GET", "/v1/webhooks/events", { ...options });
  }

  /**
   * Get a webhook
   *
   * `GET /v1/webhooks/{webhook_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:read.
   */
  getWebhook(webhookId: string, options?: RequestOptions): Promise<T.Webhook> {
    return this.json<T.Webhook>("GET", `/v1/webhooks/${encodeURIComponent(webhookId)}`, { ...options });
  }

  /**
   * Update a webhook
   *
   * `PATCH /v1/webhooks/{webhook_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:write.
   */
  updateWebhook(webhookId: string, body: T.UpdateWebhookRequest, options?: RequestOptions): Promise<T.Webhook> {
    return this.json<T.Webhook>("PATCH", `/v1/webhooks/${encodeURIComponent(webhookId)}`, { body, ...options });
  }

  /**
   * Delete a webhook
   *
   * `DELETE /v1/webhooks/{webhook_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:write.
   */
  deleteWebhook(webhookId: string, options?: RequestOptions): Promise<void> {
    return this.json<void>("DELETE", `/v1/webhooks/${encodeURIComponent(webhookId)}`, { ...options });
  }

  /**
   * Rotate the signing secret
   *
   * The new `secret` is returned once; the old one stops working immediately.
   *
   * `POST /v1/webhooks/{webhook_id}/rotate-secret`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:write.
   */
  rotateWebhookSecret(webhookId: string, options?: RequestOptions): Promise<T.Webhook> {
    return this.json<T.Webhook>("POST", `/v1/webhooks/${encodeURIComponent(webhookId)}/rotate-secret`, { ...options });
  }

  /**
   * Enqueue a `webhook.test` delivery
   *
   * `POST /v1/webhooks/{webhook_id}/test`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:write.
   */
  testWebhook(webhookId: string, options?: RequestOptions): Promise<T.TestWebhookResponse> {
    return this.json<T.TestWebhookResponse>("POST", `/v1/webhooks/${encodeURIComponent(webhookId)}/test`, { ...options });
  }

  /**
   * Enqueue a `webhook.resync` delivery with the current channel/participant snapshot
   *
   * `POST /v1/webhooks/{webhook_id}/resync`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:write.
   */
  resyncWebhook(webhookId: string, options?: RequestOptions): Promise<T.ResyncWebhookResponse> {
    return this.json<T.ResyncWebhookResponse>("POST", `/v1/webhooks/${encodeURIComponent(webhookId)}/resync`, { ...options });
  }

  /**
   * Delivery log of a webhook
   *
   * `GET /v1/webhooks/{webhook_id}/deliveries`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:read.
   */
  listWebhookDeliveries(webhookId: string, query?: T.ListWebhookDeliveriesQuery, options?: RequestOptions): Promise<T.ListWebhookDeliveriesResponse> {
    return this.json<T.ListWebhookDeliveriesResponse>("GET", `/v1/webhooks/${encodeURIComponent(webhookId)}/deliveries`, { query, ...options });
  }

  /**
   * Get a delivery
   *
   * `GET /v1/webhooks/{webhook_id}/deliveries/{delivery_id}`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:read.
   */
  getWebhookDelivery(webhookId: string, deliveryId: string, options?: RequestOptions): Promise<T.WebhookDelivery> {
    return this.json<T.WebhookDelivery>("GET", `/v1/webhooks/${encodeURIComponent(webhookId)}/deliveries/${encodeURIComponent(deliveryId)}`, { ...options });
  }

  /**
   * Retry a delivery now
   *
   * `POST /v1/webhooks/{webhook_id}/deliveries/{delivery_id}/retry`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: webhooks:write.
   */
  retryWebhookDelivery(webhookId: string, deliveryId: string, options?: RequestOptions): Promise<T.RetryWebhookDeliveryResponse> {
    return this.json<T.RetryWebhookDeliveryResponse>("POST", `/v1/webhooks/${encodeURIComponent(webhookId)}/deliveries/${encodeURIComponent(deliveryId)}/retry`, { ...options });
  }

  /**
   * Current channels and participants
   *
   * Use after `lagged` or on (re)connect to rebuild local state.
   *
   * `GET /v1/events/snapshot`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: events:read.
   */
  eventSnapshot(options?: RequestOptions): Promise<T.EventSnapshot> {
    return this.json<T.EventSnapshot>("GET", "/v1/events/snapshot", { ...options });
  }

  /**
   * TURN credentials for the calling player
   *
   * `GET /v1/me/turn-credentials`
   * Auth: PlayerToken.
   */
  myTurnCredentials(options?: RequestOptions): Promise<T.TurnCredentials> {
    return this.json<T.TurnCredentials>("GET", "/v1/me/turn-credentials", { ...options });
  }

  /**
   * Region discovery for the calling player
   *
   * Same list as `GET /v1/regions`, for the SDKs: they probe every `probe_url`, connect to the node
   * with the lowest RTT and fall back to the server's order when probing is impossible. Requires
   * only a session token.
   *
   * `GET /v1/me/regions`
   * Auth: PlayerToken.
   */
  myRegions(query?: T.MyRegionsQuery, options?: RequestOptions): Promise<T.RegionsResponse> {
    return this.json<T.RegionsResponse>("GET", "/v1/me/regions", { query, ...options });
  }

  /**
   * Report another user
   *
   * The reporter is the authenticated player.
   *
   * `POST /v1/me/reports`
   * Auth: PlayerToken.
   */
  reportUserSelf(body: T.SelfReportRequest, options?: RequestOptions): Promise<T.ModerationEvent> {
    return this.json<T.ModerationEvent>("POST", "/v1/me/reports", { body, ...options });
  }

  /**
   * Answer a recording consent request over REST
   *
   * Equivalent to the WebSocket `RecordingConsentResponse`; only the recorded participant may
   * answer.
   *
   * `POST /v1/me/recordings/{recording_id}/consent`
   * Auth: PlayerToken.
   */
  recordingConsent(recordingId: string, body: T.RecordingConsentRequest, options?: RequestOptions): Promise<T.RecordingConsentResponse> {
    return this.json<T.RecordingConsentResponse>("POST", `/v1/me/recordings/${encodeURIComponent(recordingId)}/consent`, { body, ...options });
  }

  /**
   * WebRTC signalling: SDP offer → answer
   *
   * Alternative to the WebSocket `WebRtcOffer` message. The session must have been opened over the
   * WebSocket by the same player; the SDP is limited to 64 KiB.
   *
   * `POST /v1/webrtc/offer`
   * Auth: PlayerToken.
   */
  webrtcOffer(body: T.WebRtcOfferRequest, options?: RequestOptions): Promise<T.WebRtcAnswer> {
    return this.json<T.WebRtcAnswer>("POST", "/v1/webrtc/offer", { body, ...options });
  }

  /**
   * Render a channel mixdown from participant tracks
   *
   * Combines finished per-participant tracks of one channel (aligned on `audio_started_at`, summed
   * with soft clipping) into a single Ogg/Opus or WAV file. Returns the new recording with `kind =
   * mixdown` and `status = processing`; it becomes `ready` asynchronously — poll `GET
   * /v1/recordings/{id}` or subscribe to `recording.processed`. The job runs on the node that
   * accepted the request, which must be able to read every source (its own files or object storage);
   * otherwise 409 names the node holding the track. 429 when this node's processing queue
   * (`recording.processing.max_queued`) is full.
   *
   * `POST /v1/recordings/mixdown`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:write.
   */
  mixdownRecordings(body: T.MixdownRecordingsRequest, options?: RequestOptions): Promise<T.Recording> {
    return this.json<T.Recording>("POST", "/v1/recordings/mixdown", { body, ...options });
  }

  /**
   * Queue speech-to-text of a stored recording
   *
   * Requires an `[stt]` provider on the node. A participant track is transcribed as one speaker; a
   * mixdown is transcribed from its source tracks one by one, so every segment names its speaker.
   * Audio is decoded in `recording.processing.stt_chunk_secs` chunks cut on silence and resampled to
   * `stt_sample_rate`. Returns the transcript row with `status = queued`; poll `GET .../transcript`
   * or wait for `recording.processed` with `job = transcript`. 409 while a transcript of this
   * recording is already queued/running or the recording is not `ready`; a `failed` transcript may
   * be requested again.
   *
   * `POST /v1/recordings/{recording_id}/transcribe`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:write.
   */
  transcribeRecording(recordingId: string, options?: RequestOptions): Promise<T.RecordingTranscript> {
    return this.json<T.RecordingTranscript>("POST", `/v1/recordings/${encodeURIComponent(recordingId)}/transcribe`, { ...options });
  }

  /**
   * Transcript of a recording (JSON, SRT or WebVTT)
   *
   * `format=json` (default) returns the row in any state; `srt`/`vtt` render the segments as
   * subtitles (speaker as a `<v uuid>` voice tag) and require `status = ready` (409 otherwise).
   *
   * `GET /v1/recordings/{recording_id}/transcript`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:read.
   *
   * When the server answers with application/x-subrip, text/vtt (non-JSON `format`) this call fails; use the `Raw` variant instead.
   */
  getRecordingTranscript(recordingId: string, query?: T.GetRecordingTranscriptQuery, options?: RequestOptions): Promise<T.RecordingTranscript> {
    return this.json<T.RecordingTranscript>("GET", `/v1/recordings/${encodeURIComponent(recordingId)}/transcript`, { query, ...options });
  }

  /**
   * Transcript of a recording (JSON, SRT or WebVTT)
   *
   * `format=json` (default) returns the row in any state; `srt`/`vtt` render the segments as
   * subtitles (speaker as a `<v uuid>` voice tag) and require `status = ready` (409 otherwise).
   *
   * `GET /v1/recordings/{recording_id}/transcript`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: recordings:read.
   *
   * Returns the raw body (application/json, application/x-subrip, text/vtt).
   */
  getRecordingTranscriptRaw(recordingId: string, query?: T.GetRecordingTranscriptQuery, options?: RequestOptions): Promise<RawResponse> {
    return this.raw("GET", `/v1/recordings/${encodeURIComponent(recordingId)}/transcript`, { query, ...options });
  }

  /**
   * Read markers of a user
   *
   * With `channel_id` or `peer_user_id`: the user's marker in that conversation (`null` if they
   * never read there) and how many messages from others lie after it (capped at
   * `chat.unread_count_cap`). Without a selector: every marker of the user. Requires `chat.persist =
   * true` (otherwise `404`).
   *
   * `GET /v1/users/{user_id}/read-markers`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: chat:read.
   */
  listUserReadMarkers(userId: string, query?: T.ListUserReadMarkersQuery, options?: RequestOptions): Promise<T.ReadMarkerStatus | T.ListUserReadMarkersResponseVariant1> {
    return this.json<T.ReadMarkerStatus | T.ListUserReadMarkersResponseVariant1>("GET", `/v1/users/${encodeURIComponent(userId)}/read-markers`, { query, ...options });
  }

  /**
   * Advance a user's read marker
   *
   * Marks everything in the conversation up to `message_id` as read on the user's behalf (for
   * example after a support agent answered for them). Markers only move forward: an older or equal
   * position is accepted but `moved` is `false`. The message must belong to the selected
   * conversation of that user, otherwise `404`. Every device of the user receives `ChatReadMarker`;
   * with `chat.read_receipts` so do the channel members / the peer. Requires `chat.persist = true`.
   *
   * `PUT /v1/users/{user_id}/read-markers`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: chat:write.
   */
  putUserReadMarker(userId: string, body: T.ReadMarkerRequest, options?: RequestOptions): Promise<T.PutUserReadMarkerResponse> {
    return this.json<T.PutUserReadMarkerResponse>("PUT", `/v1/users/${encodeURIComponent(userId)}/read-markers`, { body, ...options });
  }

  /**
   * Read markers in a channel
   *
   * Reading position of every user who marked something read in the channel (newest position first).
   * Requires `chat.persist = true` (otherwise `404`).
   *
   * `GET /v1/channels/{channel_id}/read-markers`
   * Auth: ApiKeyHeader | ApiKeyBearer.
   * Permissions: chat:read.
   */
  listChannelReadMarkers(channelId: string, options?: RequestOptions): Promise<T.ListChannelReadMarkersResponse> {
    return this.json<T.ListChannelReadMarkersResponse>("GET", `/v1/channels/${encodeURIComponent(channelId)}/read-markers`, { ...options });
  }

}
