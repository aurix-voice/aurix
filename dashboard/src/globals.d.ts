/// <reference types="vite/client" />

/** Injected by Vite from package.json (`vite.config.ts` → `define`). */
declare const __DASHBOARD_VERSION__: string;
/** Repository of this build's source (`vite.config.ts` → `define`, `AURIX_SOURCE_URL`). */
declare const __DASHBOARD_SOURCE_URL__: string;
